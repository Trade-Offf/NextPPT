# Disable pnpm's new-package cooldown (minimumReleaseAge) supply-chain policy.
# Our lockfile is committed and intentionally pins recent versions (e.g. zustand),
# which the default cooldown would otherwise reject in clean CI/Docker builds.
minimum-release-age=0
